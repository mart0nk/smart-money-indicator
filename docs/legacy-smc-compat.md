# Legacy SMC Compatibility

Legacy compatibility lives only under:

```ts
@trader-agent/smart-money-indicator-core/legacy
```

`src/smc/*` is a deprecated shim and contains no real implementation logic.

Compatibility output uses stable IDs:

- `id = zoneId`
- `zoneId` is included when generated by compatibility detectors
- `legacyIdWasRandom = false`

Removal target: release N+1.
